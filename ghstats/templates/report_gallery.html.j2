{% extends "report_base.html.j2" %}

{% block theme_css %}
<style>

/* Gallery Theme */
.template-gallery {
  background: #1a1a1a;
  color: #f0f0f0;
}
.template-gallery .hero {
  background: #0f0f0f;
  border: none;
  border-radius: 0;
  padding: 80px 40px;
  text-align: center;
}
.template-gallery .hero h1 {
  font-size: 5rem;
  font-weight: 900;
  text-transform: uppercase;
  color: #fff;
  letter-spacing: -2px;
}
.template-gallery .panel, .template-gallery .chart-card {
  background: #222;
  border: none;
  border-radius: 0;
  box-shadow: 20px 20px 0px rgba(0,0,0,0.5);
  margin-bottom: 20px;
}
.template-gallery .section-kicker {
  background: #315ca8;
  color: #fff;
  padding: 4px 12px;
  display: inline-block;
  transform: skewX(-10deg);
}
.template-gallery .repo-card, .template-gallery .fact {
  background: #333;
  border: none;
  border-left: 8px solid #b57f3c;
  border-radius: 0;
}

</style>
{% endblock %}
